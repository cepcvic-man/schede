## 1. Home Structure

- [x] 1.1 Refactor home markup in `index.html` so the home header is compact and operational.
- [x] 1.2 Move or restyle the home search so it appears in the primary first-viewport flow.
- [x] 1.3 Replace duplicate hero/category calls to action with one clear `Pulizia` entry point.
- [x] 1.4 Convert unavailable future categories to compact lower-priority items with `In preparazione` status.

## 2. Responsive Styling

- [x] 2.1 Update desktop CSS so the home reads as an operational dashboard rather than a tall hero layout.
- [x] 2.2 Update mobile CSS so title/status, search, and `Pulizia` entry fit before long unavailable-category content.
- [x] 2.3 Preserve CEPC brand colors, typography, focus styles, and accessible tap targets.
- [x] 2.4 Check text wrapping and spacing at narrow mobile widths.

## 3. Behavior Preservation

- [x] 3.1 Keep home search result filtering and result selection behavior working.
- [x] 3.2 Keep `Pulizia` entry opening the manual view through existing `showManual()` behavior.
- [x] 3.3 Confirm manual index, viewer iframe, mobile menu, and print behavior are not changed.

## 4. Verification

- [x] 4.1 Capture or inspect mobile home layout and confirm first viewport hierarchy.
- [x] 4.2 Capture or inspect desktop home layout and confirm dashboard hierarchy.
- [x] 4.3 Run available static checks or manual smoke tests for navigation/search.
